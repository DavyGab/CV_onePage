<div id="projets" class="content col-md-12">
    <div class="row">
        <div class="section first-section">
            <div class="page-title">
                <h2>Projets</h2>
            </div>
        </div>
        <div class="section paddT30">
            <div class="col-md-12">
                <div class="resume-education col-md-12">
                    <div class="timeline">
                        <div class="timeline-bar"></div>
                        <div class="timeline-inner clearfix">
                            {% for experience in experiences if experience.category == 3 %}
                                <div class="timeline-box timeline-box-{% if loop.index % 2 == 1 %}left{% else %}right{% endif %}">
                                    <span class="dot"></span>
                                    <div class="timeline-box-inner animate-right">
                                        <div class="ribbon ribbon-{% if loop.index % 2 == 1 %}right{% else %}left{% endif %}"><span>{{ experience.subCategory }}</span></div>
                                        <span class="arrow"></span>
                                        <div class="date">{{ experience.startDate }} - {{ experience.endDate }}</div>
                                        <h3>{{ experience.title }}</h3>
                                        <h4>{{ experience.subTitle }}</h4>
                                        <p>{{ experience.description }}</p>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {% include 'AppBundle:Default:footer.html.twig' %}
    </div>
</div>
